{
  "name": "zeit-integration-router",
  "version": "0.0.1",
  "scripts": {
    "deploy": "now --target=production",
    "dev": "concurrently \"tsc -w\" \"nodemon --exec micro dist/index.js -l tcp://localhost:5005\""
  },
  "dependencies": {
    "@zeit/integration-utils": "latest",
    "concurrently": "^4.1.0",
    "micro": "^9.3.4",
    "route-parser": "^0.0.5"
  },
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "@types/node-fetch": "^2.3.4",
    "@types/route-parser": "^0.1.3",
    "nodemon": "^1.19.1",
    "typescript": "^3.5.1"
  }
}
